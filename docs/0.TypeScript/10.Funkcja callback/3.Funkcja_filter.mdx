---
title: Funkcja `filter()`
---

# Opis
:::info .filter()
- to jak sito, przez które przesiewasz rzeczy,
- zostają tylko te elementy, które spełniają określony warunek,
- reszta zostaje odrzucona.
:::

# Metoda filter() – wyjaśnienie metaforyczne
## 1. Sito do piasku
- Masz wiadro piasku z kamykami.
- Wysypujesz zawartość na sito.
- Piasek przelatuje, a większe kamyki zostają.
- **Metafora**: `.filter()` przepuszcza tylko te elementy, które spełniają kryterium.

## 2. Klasa i sprawdzanie ocen
- Nauczyciel sprawdza listę uczniów.
- Do wycieczki jadą tylko ci, którzy mają średnią powyżej 4.0.
- **Metafora**: `.filter()` wybiera część listy, a resztę odrzuca.

## 3. Pudełko cukierków
- Masz pudełko z różnymi smakami.
- Chcesz zostawić tylko te o smaku malinowym.
- **Metafora**: `.filter()` działa jak selekcja — zostają tylko cukierki o smaku, który Cię interesuje.

---

# Przykład 1
Zwraca nową tablicę, w której są **tylko te elementy, które spełniają warunek**.

**wersja w JS**
```javascript
const numbers = [1, 2, 3, 4, 5];

// Zostawiamy tylko liczby parzyste
const even = numbers.filter(num => num % 2 === 0);

console.log(even); // [2, 4]

**wersja w TS**
```typescript
const numbers: number[] = [1, 2, 3, 4, 5];

// Zostawiamy tylko liczby parzyste
const even: number[] = numbers.filter((num: number): boolean => num % 2 === 0);

console.log(even); // [2, 4]

```

# Przykład 2 Postacie ze świata Wiedźmina
```typescript
type Character = {
  id: number;
  name: string;
  profession: string;
  level: number;
};

const characters: Character[] = [
  { id: 1, name: "Geralt", profession: "Wiedźmin", level: 45 },
  { id: 2, name: "Yennefer", profession: "Czarodziejka", level: 50 },
  { id: 3, name: "Jaskier", profession: "Bard", level: 20 }
];

// Filter na silne postacie (poziom > 30)
const strong: Character[] = characters.filter((c: Character) => c.level > 30);

console.log(strong);
/*
[
  { id: 1, name: "Geralt", profession: "Wiedźmin", level: 45 },
  { id: 2, name: "Yennefer", profession: "Czarodziejka", level: 50 }
]
*/
```

# Przykład 3: Przedmioty tańsze niż 1000
```typescript
type Item = {
    id: number;
    name: string;
    price: number;
};

const items: Item[] = [
    { id: 1, name: "Srebrny miecz", price: 1200 },
    { id: 2, name: "Eliksir Jaskółka", price: 300 },
    { id: 3, name: "Zbroja z Kaer Morhen", price: 5000 }
];

// Zostawiamy tylko tanie przedmioty
const cheapItems: Item[] = items.filter(item => item.price < 1000);

console.log(cheapItems);
/*
[
  { id: 2, name: "Eliksir Jaskółka", price: 300 }
]
*/
```


# `map()` vs `filter()`
<div className="row">
    <div className="col col--6">
        <h3>🛠 map()</h3>
        <ul>
            <li>Przekształca każdy element tablicy</li>
            <li>Zwraca <strong>tablicę o tej samej długości</strong></li>
            <li>Każdy element ma nową „wersję”</li>
            <li><code>[1,2,3].map(n => n+1)</code> → <code>[2,3,4]</code></li>
        </ul>
    </div>
    <div className="col col--6">
        <h3>🔍 filter()</h3>
        <ul>
            <li>Wybiera tylko elementy spełniające warunek</li>
            <li>Zwraca <strong>tablicę zwykle krótszą</strong></li>
            <li>Reszta elementów jest odrzucona</li>
            <li><code>[1,2,3,4].filter(n => n%2===0)</code> → <code>[2,4]</code></li>
        </ul>
    </div>
</div>






