---
title: Mongosh - same podstawy
---

# Mongosh - same podstawy


## Pierwsze kroki
| Komenda             | Opis                                                             |
| ------------------- | ---------------------------------------------------------------- |
| `show dbs`          | Wyświetla wszystkie istniejące bazy danych                       |
| `db`                | Pokazuje, w jakiej bazie danych aktualnie pracujesz              |
| `use mojaBaza`      | Przełącza się do wybranej bazy lub tworzy ją, jeśli nie istnieje |
| `db.dropDatabase()` | Usuwa **aktualnie wybraną** bazę danych                          |


```
show dbs
db
use shopDB
db.dropDatabase()
```


## Kolekcja - CR_D
🟦
### Jawne utworzenie kolekcji - CREATE
`db.createCollection("shop")`

### Automatyczne tworzenie kolekcji podczas dodawania dokumentu - CREATE
`db.users.insertOne({ name: "Jurek", age: 20 })`


### Wyświetlanie kolekcji w danej bazie - READ
`show collections`


### Usuwanie kolekcji - DELETE
`db.<nazwaKolekcji>.drop()`

np. `db.shop.drop()`


### Podsumowanie operacji na kolekcji i CRD

| Operacja | Opis | Komenda |
|----------|------|---------|
| **C — Create** | Jawne tworzenie nowej kolekcji | `db.createCollection("nazwa")` |
| **R — Read**   | Wyświetlenie listy kolekcji w bieżącej bazie | `show collections` |
| **D — Delete** | Usunięcie całej kolekcji | `db.nazwa.drop()` |




## Praca z dokumentami - CRUD

| Operacja | Opis | Komenda (przykład) |
|----------|-------|--------------------|
| **C — Create** | Dodanie nowego dokumentu | `db.users.insertOne({ name: "Jurek", age: 20 })` |
| **R — Read** | Wyszukiwanie dokumentów | `db.users.find({ age: { $gt: 18 } })` |
| **U — Update** | Aktualizacja istniejącego dokumentu | `db.users.updateOne({ name: "Jurek" }, { $set: { age: 21 } })` |
| **D — Delete** | Usuwanie dokumentów | `db.users.deleteOne({ name: "Jurek" })` |


### Przygotowanie bazy danych
```
test> use trylogia
switched to db trylogia

trylogia> db.createCollection("bohaterowie")
{ ok: 1 }
trylogia>

```
### CREATE - dodawanie dokumentów
#### Dodanie jednego dokumentu
`db.bohaterowie.insertOne({ imie: "Andrzej Kmicic", powiesc: "Potop", rola: "bohater" }) `

#### Dodanie wielu dokumentów
```
db.bohaterowie.insertMany([
{ imie: "Michał Wołodyjowski", powiesc: "Pan Wołodyjowski" },
{ imie: "Jan Skrzetuski", powiesc: "Ogniem i Mieczem" }
])
```

### READ — odczyt dokumentów

#### Odczyt wszystkich dokumentów
`db.bohaterowie.find()`

#### Filtrowanie
`db.bohaterowie.find({powiesc:"Potop"})`

#### Wyszukiwanie konkretnej postaci
`db.bohaterowie.find({imie:"Andrzej Kmicic"})`

### UPDATE - aktualizacja dokumentów
#### Zmiana wartości istniejącego pola
`db.bohaterowie.updateOne({imie:"Andrzej Kmicic"}, {$set: {imie:"ANDRZEJ KMICIC"}})`

#### Dodanie nowego pola do dokumentu
**dla wszystkich dokumentów**
`db.bohaterowie.updateMany({}, {$set: {epoka: "Barok"}})`

**dla wybranych dokumentów**
`db.bohaterowie.updateMany({imie:'Michał Wołodyjowski'}, {$set: {czyLubiany:true}})`


### DELETE - usuwanie dokumentu
`db.bohaterowie.deleteOne({ imie: "Michał Wołodyjowski" })`

`db.bohaterowie.deleteMany({epoka:"Barok"})`


























